---
description: Coding standards for Node.js ESM and TS/JS hybrid projects.
alwaysApply: true
---
# Node + TS/JS Standards

- Use ESM imports/exports (`type: module`).
- Prefer TypeScript for domain logic and shared contracts.
- Use JavaScript adapters only where interoperability is intentional.
- Keep public feature entrypoints explicit through `index.ts` files.
- Keep functions small and deterministic when possible.
