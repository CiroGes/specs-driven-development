# Tasks: <feature-name>

<!-- Reference the acceptance-criteria ids each task covers, e.g. (AC1, AC2).
     coverage:specs uses these refs to prove every criterion has a task.
     Tasks with no AC reference (chores/setup/docs) are reported as warnings. -->

- [ ] Confirm spec is complete and approved.
- [ ] Implement domain/service logic. (AC<n>)
- [ ] Implement adapter/controller layer. (AC<n>)
- [ ] Add unit tests for service logic. (AC<n>)
- [ ] Add integration tests for feature flow. (AC<n>)
- [ ] (If acceptance requires manual CLI verification) Add or reuse a project script for manual verification; document the command in `acceptance.md`.
- [ ] Update traceability in spec docs.
